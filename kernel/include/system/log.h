#pragma once

#include <ak/types.h>

namespace pranaOS {
    namespace system {
        #define LOG_SHOW_MS 1
        
        enum LogLevel { 
            Info,
            Warning,
            Error
        };
    }
}