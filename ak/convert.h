#pragma once

#include "types.h"
#include "memoperator.h"

namespace pranaOS {
    namespace ak {
        class Convert {
        public:
            static char* intToString(int i);
        }
    }
}