import("//build/userland/TEMPLATE.gni")

pranaOS_executable("getenv") {
  install_path = "bin/"
  sources = [
    "getenv.c",
  ]
  configs = [ "//build/userland:userland_flags" ]
  deplibs = [
    "libcxx",
    "libfoundation",
    "libg",
    "libui",
  ]
}
