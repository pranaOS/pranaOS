import("//build/userland/TEMPLATE.gni")

pranaOS_executable("fontcompiler") {
  install_path = "bin/"
  sources = [
    "main.cpp",
    "common.h",
  ]
  configs = [ "//build/userland:userland_flags" ]
  deplibs = [
    "libcxx",
    "libfoundation",
    "libg",
    "libui",
  ]
}
