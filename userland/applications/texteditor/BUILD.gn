import("//build/userland/TEMPLATE.gni")

pranaOS_application("texteditor") {
  sources = [
    "AppDelegate.cpp",
    "TextEditorView.cpp",
  ]
  configs = [ "//build/userland:userland_flags" ]
  deplibs = [
    "libcxx",
    "libfoundation",
    "libg",
    "libui",
  ]
}
