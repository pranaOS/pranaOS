#pragma once 

#include <ak/types.h>

namespace Bootloader {
    class bootingMsg {
        public:
            static uint32_t bootingTime;
    
        private:
            ~bootingMsg();
 
    };
}