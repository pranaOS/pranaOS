import("//build/libs/TEMPLATE.gni")

pranaOS_static_library("libnet") {
  sources = [
    "arp.h"
  ]

  deplibs = [ "libcxx" ]
  configs = [ "//build/libs:libcxx_flags" ]

  if (host == "llvm") {
    cflags = [ "-flto" ]
  }
}
