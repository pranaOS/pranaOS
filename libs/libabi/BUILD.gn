import("//build/libs/TEMPLATE.gni")

pranaOS_static_library("libabi") {
  sources = [
    "process.h",
    "filesystem.h",
    "network.h",
    "time.h",
  ]

  deplibs = [
    "libcxx",
    "libipc",
  ]
  configs = [ "//build/libs:libcxx_flags" ]

  if (host == "llvm") {
    cflags = [ "-flto" ]
  }
}
