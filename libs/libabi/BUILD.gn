import("//build/libs/TEMPLATE.gni")

pranaOS_static_library("libabi") {
  sources = [
    "keyboard.h",
    "iocall.h",
    "process.h",
    "filesystem.h",
    "network.h",
    "time.h",
    "result.h",
    "rune.h",
    "memory.h",
  ]

  deplibs = [
    "libcxx",
    "libfoundation",
    "libg",
    "libipc",
  ]
  configs = [ "//build/libs:libcxx_flags" ]

  if (host == "llvm") {
    cflags = [ "-flto" ]
  }
}
