import("//build/libs/TEMPLATE.gni")

pranaOS_static_library("libutils") {
  sources = [
    "Prelude.h",
    "Macros.h",
    "MinMax.h",
  ]

  deplibs = [
    "libcxx",
    "libc",
  ]
  configs = [ "//build/libs:libcxx_flags" ]

  if (host == "llvm") {
    cflags = [ "-flto" ]
  }
}
